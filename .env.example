# Environment Variables for HNBrief
# Copy this file to .env and fill in your values

# ========== LLM Provider Configuration ==========
# Priority: Cebras > Nvidia NIM > Xiaomi MiMo > OpenRouter > OpenAI-compatible
# Only ONE provider needs to be configured (the first available will be used)

# Option 1: Cebras (Primary)
CEBRAS_API_KEY=your_cebras_api_key_here
# CEBRAS_API_URL=https://api.cerebras.ai/v1/chat/completions  # Optional override
CEBRAS_API_MODEL=qwen-3-235b-a22b-instruct-2507

# Option 2: Nvidia NIM (Fallback)
# Uses Moonshot/Kimi K2.5 via Nvidia's API
# Note: Can be unstable with timeouts
NVIDIA_API_KEY=your_nvidia_api_key_here
NVIDIA_MODEL=moonshotai/kimi-k2.5

# Option 3: Xiaomi MiMo (Fallback)
XIAOMI_API_KEY=your_xiaomi_api_key_here
XIAOMI_API_URL=https://api.xiaomimimo.com/v1/chat/completions
XIAOMI_MODEL=mimo-v2-flash

# Option 4: OpenRouter (Last resort)
# Free tier available, no credit card required
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=arcee-ai/trinity-large-preview:free
# Other free models to try:
# - arcee-ai/trinity-large-preview:free
# - x-ai/grok-2-1212:free
# - deepseek/deepseek-chat:free

# Option 5: OpenAI-compatible (requires explicit configuration)
# OPENAI_API_KEY=your_openai_compatible_key_here
# LLM_API_URL=https://api.openai.com/v1/chat/completions
# LLM_MODEL=gpt-4o-mini

# ========== Thinking Mode Control (Optional) ==========
# Only affects article summaries and digest generation
# Health checks always use instant mode for speed

# LLM_THINKING_FORCE=true  # Enable thinking for both articles and digest
# LLM_THINKING=true        # Alias for LLM_THINKING_FORCE

# ========== Cloudflare Worker (optional) ==========
# Only needed for automated daily runs via Cloudflare Worker

GITHUB_TOKEN=your_github_personal_access_token
REPO_OWNER=your_github_username
REPO_NAME=hnbrief

# ========== TTS Worker (optional) ==========
# Only needed for text-to-speech audio generation

MINIMAX_API_KEY=your_minimax_api_key_here
PODCAST_HOST_A_VOICE=Expressive_Narrator   # British male voice
PODCAST_HOST_B_VOICE=Compelling_Lady       # British female voice

# ========== Comment Selection (optional) ==========
# Configure how comments are selected for summarization

MAX_COMMENT_CHARS=15000        # Character budget for comments (~10-15 top comments)
MAX_COMMENTS_PER_ROOT=3        # Max comments from same top-level thread
